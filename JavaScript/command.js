var Command = function(execute, [undo], dest, src){
    this.execute = execute;
    this.undo = undo;
    this.dest = dest;
    this.src = src;
}


var init = function(name){
    var initList = {
        'chat': 'chat-plugin',
        'reply': 'reply-plugin'
    };


}
